import { defineMessages } from 'react-intl';

export const scope = 'app.carousel.FullWidthCarousel';

export default defineMessages({
  link: {
    id: `${scope}.link`,
    defaultMessage: 'Ver programa',
  },
});
