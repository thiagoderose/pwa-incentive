import { defineMessages } from 'react-intl';

const scope = 'app.components.ShareDialog';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Compartilhar',
  },
});
