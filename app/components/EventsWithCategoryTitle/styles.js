const styles = (theme) => ({
  title: {
    marginTop: theme.spaces.loose.value,
    marginBottom: theme.spaces.loose.value,
  },

  list: {
    paddingBottom: theme.spaces.loose.value,
  },
});

export default styles;
