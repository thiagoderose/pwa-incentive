import { defineMessages } from 'react-intl';

const scope = 'app.components.FooterContact';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Contact us',
  },
});
